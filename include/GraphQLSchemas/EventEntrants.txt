query EventStandingsQuery($slug: String!) {
  event(slug: $slug) {
    id
    entrants(query: {perPage: 192}) {
      nodes {
        id
        name
        participants {
          user {
            id
            player {
              id
              gamerTag
            }
          }
        }
      }
    }
  }
}
