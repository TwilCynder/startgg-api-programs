query EventsFromUser($id: ID, $slug: String, $eventsPage: Int, $eventsPerPage: Int, $games: [Int]) {
	user(id: $id, slug: $slug) {
    events (query : {
      page: $eventsPage,
      sortBy: "startAt desc",
      perPage: $eventsPerPage,
      filter: {
        videogameId: 
      }
    }){
      nodes {
        slug
        tournament {
          name
        }
        entrantSizeMin
        id
        startAt
        numEntrants
      }
    }
  }	
}